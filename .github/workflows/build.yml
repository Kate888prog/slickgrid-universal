name: Simple Angular Download

on:
  workflow_dispatch:

jobs:
  download:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
      
    - name: Create downloadable package
      run: |
        # Просто архивируем папку angular-slickgrid
        cd frameworks/angular-slickgrid
        zip -r angular-slickgrid-source.zip .
        echo "✅ Архив создан!"
        ls -la *.zip
      
    - name: Upload for download
      uses: actions/upload-artifact@v4
      with:
        name: angular-slickgrid-source-code
        path: frameworks/angular-slickgrid/angular-slickgrid-source.zip
        retention-days: 30
        
    - name: Done
      run: |
        echo "🎉 ГОТОВО!"
        echo "Скачайте 'angular-slickgrid-source-code' - это исходный код Angular библиотеки"