name: Build Angular SlickGrid

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
      
    - name: Setup Node.js
      uses: actions/setup-node@v4
      with:
        node-version: '20'
        
    - name: Install pnpm
      run: npm install -g pnpm
      
    - name: Install root dependencies
      run: pnpm install
      
    - name: Build Angular
      run: |
        cd frameworks/angular-slickgrid
        pnpm install
        pnpm run build
      
    # ⬇️⬇️⬇️ ТЕПЕРЬ АРТЕФАКТЫ БУДУТ РАБОТАТЬ ⬇️⬇️⬇️
    
    - name: Show build results
      run: |
        cd frameworks/angular-slickgrid/dist
        echo "✅ Angular успешно собран!"
        echo "📁 Содержимое папки dist:"
        ls -la
        echo "📊 Размеры файлов:"
        du -h *
        
    - name: Create ZIP archive
      run: |
        cd frameworks/angular-slickgrid/dist
        zip -r angular-slickgrid-build.zip .
        echo "📦 Создан ZIP архив"
        du -h angular-slickgrid-build.zip
        
    - name: Upload ZIP archive
      uses: actions/upload-artifact@v4
      with:
        name: angular-slickgrid-compiled
        path: frameworks/angular-slickgrid/dist/angular-slickgrid-build.zip
        retention-days: 30
        
    - name: Upload dist folder
      uses: actions/upload-artifact@v4
      with:
        name: angular-dist-files
        path: frameworks/angular-slickgrid/dist/
        retention-days: 30
        
    - name: Success message
      run: |
        echo "🎉 ВСЁ ГОТОВО!"
        echo ""
        echo "📥 КАК СКАЧАТЬ:"
        echo "1. Вернитесь на страницу этого workflow"
        echo "2. Прокрутите ВНИЗ до конца страницы"
        echo "3. Найдите раздел 'Artifacts'"
        echo "4. Нажмите:"
        echo "   - 'angular-slickgrid-compiled' для ZIP архива"
        echo "   - 'angular-dist-files' для папки с файлами"
        echo ""
        echo "⏰ Файлы будут доступны 30 дней"