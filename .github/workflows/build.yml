name: 📦 Download Source Code Only

on:
  workflow_dispatch:

jobs:
  download-source:
    runs-on: ubuntu-latest
    
    steps:
    - name: 📥 Checkout code
      uses: actions/checkout@v4
      
    - name: 📦 Create source archive
      run: |
        # Просто архивируем Angular папку
        zip -r angular-slickgrid-source.zip \
          frameworks/angular-slickgrid/ \
          -x "*/node_modules/*" "*/dist/*"
        
        echo "📦 Исходный код: $(du -h angular-slickgrid-source.zip)"
        echo "✅ Готово! Это чистый исходный код без сборки"
      
    - name: ⬆️ Upload source
      uses: actions/upload-artifact@v4
      with:
        name: angular-slickgrid-source-code
        path: angular-slickgrid-source.zip
